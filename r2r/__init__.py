"""r2r — the Reward2Reason course infrastructure.

This package is the course's "hardware simulator": environments, a tiny
tensor library, an arena, and built-in reference agents. Students never
modify this package — they build their own components in modules/, and
the tests in tests/ verify them against these environments.

The Built-In Chip Rule (borrowed from Nand2Tetris): once you have built a
component yourself and its tests pass, you have earned the right to use
the optimized built-in version here. Never before.
"""

__version__ = "1.0.0"
